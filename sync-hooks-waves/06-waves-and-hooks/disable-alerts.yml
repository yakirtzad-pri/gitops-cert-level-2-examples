apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    argocd.argoproj.io/hook: PretSync
    argocd.argoproj.io/sync-wave: "20"
  namespace: example06
  generateName: disable-alerts-
  labels:
    codefresh.io/cert: disable-job  
spec:
  template:
    spec:
      containers:
      - name: dummy-run
        image: alpine
        command:
          - "sleep"
          - "10"
      restartPolicy: Never
  backoffLimit: 2    
